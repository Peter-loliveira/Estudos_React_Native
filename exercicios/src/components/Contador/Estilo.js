import { StyleSheet } from 'react-native'

export default StyleSheet.create({

    linha: {
        width: 300,
        flex:1,
        justifyContent: 'space-between',
        flexDirection: 'row',
        alignItems: 'center'
    }
}
)